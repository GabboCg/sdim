# He et al. (2023) Replication Data Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add six lazy-load datasets from the He et al. (2023) replication package (`Portfolio_ret.xlsx`) to the `sdim` R package.

**Architecture:** A `data-raw/he2023.R` script reads the Excel file (located one level above the package root) and writes six compressed `.rda` files into `data/`. Each dataset is a `data.frame` with a `Date` column plus numeric return columns. Datasets are documented in `R/data.R` with full roxygen blocks. `DESCRIPTION` is updated to enable lazy-loading and declare `readxl` as a `Suggests` dependency.

**Tech Stack:** Base R + `readxl` (build-time only). Tests use `testthat` edition 3. Development via `devtools::load_all()` / `devtools::test()` run from inside `sdim/sdim/`.

**Spec:** `docs/superpowers/specs/2026-04-04-he2023-data-design.md`

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `data-raw/he2023.R` | Reads Excel, processes 6 sheets, writes `.rda` files |
| Create | `data/he2023_factors.rda` … `data/he2023_dacheng202.rda` | Lazy-load dataset binaries (6 files, generated by the script) |
| Create | `R/data.R` | roxygen documentation for all 6 datasets |
| Create | `tests/testthat/test-data-he2023.R` | Dimension, date, type, and naming assertions for all 6 datasets |
| Modify | `DESCRIPTION` | `LazyData: true`; add `readxl` to `Suggests` |
| Modify | `.Rbuildignore` | Add `^data-raw$` |

---

## Task 1: DESCRIPTION and .Rbuildignore

**Files:**
- Modify: `sdim/sdim/DESCRIPTION`
- Modify: `sdim/sdim/.Rbuildignore`

- [ ] **Step 1: Update `LazyData` in DESCRIPTION**

In `sdim/sdim/DESCRIPTION`, change:
```
LazyData: false
```
to:
```
LazyData: true
```

- [ ] **Step 2: Add `readxl` to Suggests in DESCRIPTION**

Change:
```
Suggests: frenchdata, knitr, rmarkdown, testthat (>= 3.0.0)
```
to:
```
Suggests: frenchdata, knitr, readxl, rmarkdown, testthat (>= 3.0.0)
```

- [ ] **Step 3: Add `^data-raw$` to .Rbuildignore**

Append to `sdim/sdim/.Rbuildignore`:
```
^data-raw$
```

- [ ] **Step 4: Commit**

Run from the **repository root** (parent of `sdim/`):

```bash
git add sdim/sdim/DESCRIPTION sdim/sdim/.Rbuildignore
git commit -m "chore: enable LazyData, add readxl to Suggests, exclude data-raw from tarball"
```

---

## Task 2: Processing script and `.rda` files

**Files:**
- Create: `sdim/sdim/data-raw/he2023.R`
- Create: `sdim/sdim/data/` (6 `.rda` files generated by the script)

- [ ] **Step 5: Create `data-raw/` directory and write the script**

Create `sdim/sdim/data-raw/he2023.R` with exactly this content:

```r
# data-raw/he2023.R
# Generates he2023_* datasets from the He et al. (2023) replication package.
# Run from the package root (sdim/) with source("data-raw/he2023.R").
# Requires: readxl

if (!requireNamespace("readxl", quietly = TRUE))
  stop("Package 'readxl' is needed to run this script. Install it with install.packages('readxl').")

xl <- "../refs/replication_package_MS-FIN-21-01990/data/Portfolio_ret.xlsx"

stopifnot(file.exists(xl))

# Helper: yyyymm integer -> Date (first of month)
ym_to_date <- function(x) as.Date(paste0(as.integer(x), "01"), "%Y%m%d")

# ---- he2023_factors (Anomaly sheet) ---------------------------------------
# All five non-Dacheng sheets share an identical two-row header:
#   Excel row 1: NA + numeric column indices (1, 2, 3, ...)
#   Excel row 2: actual labels (MKT, SMB, HML, ... / Agric, Food, ...)
# skip = 1 skips the index row; readxl uses row 2 as column headers.
raw <- readxl::read_excel(xl, sheet = "Anomaly", skip = 1)
nms <- gsub("\\\\", "", colnames(raw))   # strip backslash from 5 factor names
colnames(raw) <- nms
he2023_factors <- data.frame(
  date = ym_to_date(raw[[1]]),
  lapply(raw[-1], as.numeric),
  check.names = FALSE
)

# ---- FF industry portfolios -----------------------------------------------
# FF5 sheet intentionally omitted (different sample period, not used as estimator input).
for (sh in c("FF48vw", "FF30vw", "FF17vw", "FF48ew")) {
  raw          <- readxl::read_excel(xl, sheet = sh, skip = 1)
  colnames(raw)[1] <- "ym"
  df <- data.frame(
    date = ym_to_date(raw$ym),
    lapply(raw[-1], as.numeric),
    check.names = FALSE
  )
  assign(paste0("he2023_", tolower(sh)), df)
}

# ---- he2023_dacheng202 ----------------------------------------------------
# Sheet has two header rows: row 1 = group labels (sparse), row 2 = within-group indices.
raw       <- readxl::read_excel(xl, sheet = "Dacheng202vw", col_names = FALSE)
data_rows <- raw[-(1:2), ]
ym_col    <- as.integer(data_rows[[1]])
ret_cols  <- lapply(data_rows[-1], as.numeric)
names(ret_cols) <- sprintf("p%03d", seq_along(ret_cols))
he2023_dacheng202 <- data.frame(
  date = ym_to_date(ym_col),
  ret_cols,
  check.names = FALSE
)

# ---- Save all datasets ----------------------------------------------------
if (!dir.exists("data")) dir.create("data")
datasets <- c("he2023_factors", "he2023_ff48vw", "he2023_ff30vw",
              "he2023_ff17vw", "he2023_ff48ew", "he2023_dacheng202")
for (nm in datasets) {
  save(list = nm, file = paste0("data/", nm, ".rda"),
       envir = environment(), compress = "xz")
}
message("Done. Run devtools::document() to update NAMESPACE if needed.")
```

- [ ] **Step 6: Run the script to generate the `.rda` files**

From an R session with working directory set to `sdim/sdim/`:

```r
source("data-raw/he2023.R")
```

Expected output:
```
Done. Run devtools::document() to update NAMESPACE if needed.
```

Verify the files were created:

```r
list.files("data/", pattern = "^he2023")
# Expected: 6 files — he2023_dacheng202.rda  he2023_factors.rda
#                       he2023_ff17vw.rda     he2023_ff30vw.rda
#                       he2023_ff48ew.rda     he2023_ff48vw.rda
```

- [ ] **Step 7: Commit**

Run from the **repository root**:

```bash
git add sdim/sdim/data-raw/he2023.R sdim/sdim/data/
git commit -m "data: generate he2023_* datasets from replication package"
```

---

## Task 3: Test file

**Files:**
- Create: `sdim/sdim/tests/testthat/test-data-he2023.R`

- [ ] **Step 8: Write the test file**

Create `sdim/sdim/tests/testthat/test-data-he2023.R` with exactly this content:

```r
test_that("he2023_factors has correct dimensions, date class, and date range", {
  expect_equal(nrow(he2023_factors), 516L)
  expect_equal(ncol(he2023_factors), 71L)
  expect_s3_class(he2023_factors$date, "Date")
  expect_equal(he2023_factors$date[1],   as.Date("1974-01-01"))
  expect_equal(he2023_factors$date[516], as.Date("2016-12-01"))
  expect_type(he2023_factors$MKT, "double")
  # Verify backslash stripping was applied
  expect_true("F_g7" %in% names(he2023_factors))
  expect_false(any(grepl("\\", names(he2023_factors), fixed = TRUE)))
})

test_that("he2023_dacheng202 has correct dimensions, column names, and date range", {
  expect_equal(nrow(he2023_dacheng202), 552L)
  expect_equal(ncol(he2023_dacheng202), 203L)
  expect_equal(names(he2023_dacheng202)[1],   "date")
  expect_equal(names(he2023_dacheng202)[2],   "p001")
  expect_equal(names(he2023_dacheng202)[203], "p202")
  expect_s3_class(he2023_dacheng202$date, "Date")
  expect_equal(he2023_dacheng202$date[1],   as.Date("1972-01-01"))
  expect_equal(he2023_dacheng202$date[552], as.Date("2017-12-01"))
})

test_that("he2023_ff48vw has correct dimensions and date range", {
  expect_equal(nrow(he2023_ff48vw), 528L)
  expect_equal(ncol(he2023_ff48vw), 49L)
  expect_s3_class(he2023_ff48vw$date, "Date")
  expect_equal(he2023_ff48vw$date[1],   as.Date("1974-01-01"))
  expect_equal(he2023_ff48vw$date[528], as.Date("2017-12-01"))
})

test_that("he2023_ff30vw has correct dimensions and date range", {
  expect_equal(nrow(he2023_ff30vw), 528L)
  expect_equal(ncol(he2023_ff30vw), 31L)
  expect_s3_class(he2023_ff30vw$date, "Date")
  expect_equal(he2023_ff30vw$date[1],   as.Date("1974-01-01"))
  expect_equal(he2023_ff30vw$date[528], as.Date("2017-12-01"))
})

test_that("he2023_ff17vw has correct dimensions and date range", {
  expect_equal(nrow(he2023_ff17vw), 528L)
  expect_equal(ncol(he2023_ff17vw), 18L)
  expect_s3_class(he2023_ff17vw$date, "Date")
  expect_equal(he2023_ff17vw$date[1],   as.Date("1974-01-01"))
  expect_equal(he2023_ff17vw$date[528], as.Date("2017-12-01"))
})

test_that("he2023_ff48ew has correct dimensions and date range", {
  expect_equal(nrow(he2023_ff48ew), 528L)
  expect_equal(ncol(he2023_ff48ew), 49L)
  expect_s3_class(he2023_ff48ew$date, "Date")
  expect_equal(he2023_ff48ew$date[1],   as.Date("1974-01-01"))
  expect_equal(he2023_ff48ew$date[528], as.Date("2017-12-01"))
})
```

- [ ] **Step 9: Load package and run the new tests**

From an R session inside `sdim/sdim/`:

```r
devtools::load_all()
testthat::test_file("tests/testthat/test-data-he2023.R")
```

Expected: all 6 tests pass.

- [ ] **Step 10: Run full test suite to confirm no regressions**

```r
devtools::test()
```

Expected: all tests pass across all test files.

- [ ] **Step 11: Commit**

Run from the **repository root**:

```bash
git add sdim/sdim/tests/testthat/test-data-he2023.R
git commit -m "test: add he2023 dataset dimension, date, and type assertions"
```

---

## Task 4: Documentation (`R/data.R`)

**Files:**
- Create: `sdim/sdim/R/data.R`

- [ ] **Step 12: Create `R/data.R` with roxygen blocks for all 6 datasets**

Create `sdim/sdim/R/data.R` with exactly this content:

```r
#' Factor proxies from He, Huang, Li, Zhou (2023)
#'
#' Monthly returns on 70 anomaly-based long-short factor portfolios from the
#' replication package of He, Huang, Li, Zhou (2023). Used as factor proxies
#' (\code{X}) in the RRA, PLS, and PCA estimators.
#'
#' @format A data.frame with 516 rows and 71 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{MKT}{Market excess return (percent).}
#'   \item{SMB}{Small-minus-big size factor (percent).}
#'   \item{HML}{High-minus-low value factor (percent).}
#'   \item{...}{67 additional anomaly factors (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_factors[, 1:5])
"he2023_factors"

#' Fama-French 48-industry value-weighted portfolios from He, Huang, Li, Zhou (2023)
#'
#' Monthly value-weighted returns on the 48 Fama-French industry portfolios from
#' the replication package of He, Huang, Li, Zhou (2023). Used as the target
#' return matrix (\code{target}) in the RRA, PLS, and PCA estimators.
#'
#' @format A data.frame with 528 rows and 49 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{Agric}{Agriculture portfolio return (percent).}
#'   \item{Food}{Food products portfolio return (percent).}
#'   \item{...}{46 additional industry portfolio returns (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_ff48vw[, 1:5])
"he2023_ff48vw"

#' Fama-French 30-industry value-weighted portfolios from He, Huang, Li, Zhou (2023)
#'
#' Monthly value-weighted returns on the 30 Fama-French industry portfolios from
#' the replication package of He, Huang, Li, Zhou (2023). Used as the target
#' return matrix (\code{target}) in the RRA, PLS, and PCA estimators.
#'
#' @format A data.frame with 528 rows and 31 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{...}{30 industry portfolio returns (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_ff30vw[, 1:5])
"he2023_ff30vw"

#' Fama-French 17-industry value-weighted portfolios from He, Huang, Li, Zhou (2023)
#'
#' Monthly value-weighted returns on the 17 Fama-French industry portfolios from
#' the replication package of He, Huang, Li, Zhou (2023). Used as the target
#' return matrix (\code{target}) in the RRA, PLS, and PCA estimators.
#'
#' @format A data.frame with 528 rows and 18 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{...}{17 industry portfolio returns (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_ff17vw[, 1:5])
"he2023_ff17vw"

#' Fama-French 48-industry equal-weighted portfolios from He, Huang, Li, Zhou (2023)
#'
#' Monthly equal-weighted returns on the 48 Fama-French industry portfolios from
#' the replication package of He, Huang, Li, Zhou (2023). Used as the target
#' return matrix (\code{target}) in the RRA, PLS, and PCA estimators.
#'
#' @format A data.frame with 528 rows and 49 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{Agric}{Agriculture portfolio return (percent).}
#'   \item{Food}{Food products portfolio return (percent).}
#'   \item{...}{46 additional industry portfolio returns (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_ff48ew[, 1:5])
"he2023_ff48ew"

#' Dacheng 202-portfolio value-weighted returns from He, Huang, Li, Zhou (2023)
#'
#' Monthly value-weighted returns on 202 portfolios (from Dacheng Xiu's
#' replication data) from the replication package of He, Huang, Li, Zhou (2023).
#' Used as the target return matrix (\code{target}) in the RRA, PLS, and PCA
#' estimators. Columns are named sequentially \code{p001}--\code{p202}.
#'
#' @format A data.frame with 552 rows and 203 variables:
#' \describe{
#'   \item{date}{First day of each month, class \code{Date}.}
#'   \item{p001}{Portfolio 1 return (percent).}
#'   \item{p002}{Portfolio 2 return (percent).}
#'   \item{...}{Portfolios p003 through p202 (percent).}
#' }
#' @source He, Huang, Li, Zhou (2023) replication package,
#'   \url{https://pubsonline.informs.org/doi/10.1287/mnsc.2022.4428}.
#' @references He, J., Huang, J., Li, F., and Zhou, G. (2023).
#'   Shrinking Factor Dimension: A Reduced-Rank Approach.
#'   \emph{Management Science}, 69(9).
#'   \doi{10.1287/mnsc.2022.4428}
#' @keywords datasets
#' @examples
#' head(he2023_dacheng202[, 1:5])
"he2023_dacheng202"
```

- [ ] **Step 13: Regenerate documentation**

From an R session inside `sdim/sdim/`:

```r
devtools::document()
```

Expected: 6 new `.Rd` files created in `man/`:
`man/he2023_factors.Rd`, `man/he2023_ff48vw.Rd`, `man/he2023_ff30vw.Rd`,
`man/he2023_ff17vw.Rd`, `man/he2023_ff48ew.Rd`, `man/he2023_dacheng202.Rd`

- [ ] **Step 14: Run full test suite**

```r
devtools::test()
```

Expected: all tests pass.

- [ ] **Step 15: Commit**

Run from the **repository root**:

```bash
git add sdim/sdim/R/data.R sdim/sdim/man/
git commit -m "docs: add roxygen documentation for he2023_* datasets"
```

---

## Task 5: Verify package check

- [ ] **Step 16: Run R CMD CHECK**

From an R session inside `sdim/sdim/`:

```r
devtools::check()
```

Expected: 0 errors, 0 warnings. Pre-existing NOTEs about placeholder author and missing vignette are acceptable.

- [ ] **Step 17: Commit any auto-fixes if needed, otherwise done**

If `devtools::check()` made any changes (e.g., to `NAMESPACE`), commit them:

```bash
git add sdim/sdim/NAMESPACE sdim/sdim/man/
git commit -m "chore: post-check fixes after adding he2023 datasets"
```
